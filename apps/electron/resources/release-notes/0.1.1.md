# Craft Agents OSS 0.1.1

Fixes the first GitHub-built desktop packages.

- Bundle the Claude Agent SDK runtime module into packaged Electron apps.
- Bundle the target-architecture native Claude binary during `afterPack`.
- Fail CI packaging if required SDK or ripgrep runtime files are missing.
